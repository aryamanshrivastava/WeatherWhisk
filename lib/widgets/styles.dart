import 'package:flutter/material.dart';

class Styles {
  static const defaultThickFontSize = 15.0;
  static const double defaultPadding = 15;
  static const double defaultDividerSpace = 20;
  static const defaultThinFont = TextStyle(
      fontFamily: "RedHat",
      fontWeight: FontWeight.bold,
      fontSize: 12,
      color: Colors.black);
  static const defaultIconSize = 20;
  static const paddingbig = 30.0;
  static const cursorColor = Colors.grey;
  static const borderRadius = 50.0;
  static const labelColor = Colors.grey;
  static const borderColor = Colors.black;
  static const iconWidth = 40.0;
  static const iconHight = 40.0;
  static const mainFontSize = 120.0;
}
